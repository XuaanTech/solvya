---
title: "HEX, RGB, HSL y CMYK: conversión completa para diseño e impresión"
titleEn: "HEX, RGB, HSL and CMYK: Complete Conversion for Design and Print"
description: "Convierte entre HEX, RGB, HSL y CMYK. Entiende gamut, perfiles de color, por qué el CMYK no cubre todo el RGB y cómo preparar archivos para imprenta."
descriptionEn: "Convert between HEX, RGB, HSL and CMYK. Understand gamut, color profiles, why CMYK doesn't cover all RGB and how to prepare files for print."
category: conversores
categoryName: Conversores
tags: [hex rgb hsl cmyk, conversion color, gamut, perfil color, impresion, diseno]
date: 2026-09-13
author: "Equipo Solvya"
tool: color
faqs:
  - q:
      es: "¿Por qué mis colores RGB se ven apagados al convertir a CMYK?"
      en: "Why do my RGB colors look dull when converted to CMYK?"
    a:
      es: "RGB (luz) tiene **gamut más amplio** que CMYK (tinta). Colores muy saturados (azules, verdes, naranjas brillantes) están **fuera del gamut CMYK** y se recortan al convertir. Usa perfil FOGRA39/ISO Coated v2 y revisa 'advertencia de gamut' en Photoshop/Illustrator."
      en: "RGB (light) has a **wider gamut** than CMYK (ink). Highly saturated colors (bright blues, greens, oranges) fall **outside the CMYK gamut** and get clipped on conversion. Use FOGRA39/ISO Coated v2 profile and check 'gamut warning' in Photoshop/Illustrator."
  - q:
      es: "¿Puedo convertir HEX directamente a CMYK?"
      en: "Can I convert HEX directly to CMYK?"
    a:
      es: "HEX → RGB (directo) → CMYK (requiere perfil de color). No existe conversión matemática única HEX→CMYK sin perfil; el resultado depende del papel, tinta e imprenta. Usa el conversor de color con perfil FOGRA39 para simular."
      en: "HEX → RGB (direct) → CMYK (requires color profile). No single mathematical HEX→CMYK conversion exists without a profile; result depends on paper, ink and press. Use the color converter with FOGRA39 profile to simulate."
steps:
  - title:
      es: "Diseña en RGB (gamut amplio) para pantalla"
      en: "Design in RGB (wide gamut) for screen"
    text:
      es: "Trabaja en sRGB (web) o Display P3 (pantallas amplias). Usa HEX/RGB/HSL intercambiablemente. Nuestro conversor HEX↔RGB↔HSL es exacto para pantalla."
      en: "Work in sRGB (web) or Display P3 (wide displays). Use HEX/RGB/HSL interchangeably. Our HEX↔RGB↔HSL converter is exact for screen."
  - title:
      es: "Prepara para imprenta: convierte a CMYK con perfil"
      en: "Prepare for print: convert to CMYK with profile"
    text:
      es: "Asigna perfil CMYK (FOGRA39 / ISO Coated v2 300 % / US Web Coated SWOP v2 según imprenta). Activa 'advertencia de gamut' y ajusta colores fuera de gama. Entrega PDF/X-1a o PDF/X-4 con perfil incrustado."
      en: "Assign CMYK profile (FOGRA39 / ISO Coated v2 300 % / US Web Coated SWOP v2 per printer). Enable 'gamut warning' and adjust out-of-gamut colors. Deliver PDF/X-1a or PDF/X-4 with embedded profile."
  - title:
      es: "Prueba de color (soft proof / hard proof)"
      en: "Color proof (soft proof / hard proof)"
    text:
      es: "Soft proof: simula en pantalla con perfil impresora + papel. Hard proof: prueba impresa en papel final (certificada ISO 12647-7). Aprueba antes de tirada."
      en: "Soft proof: simulate on screen with printer+paper profile. Hard proof: printed proof on final paper (ISO 12647-7 certified). Approve before run."
---

**HEX, RGB, HSL** son para **pantalla (luz)**; **CMYK** es para **impresión (tinta)**. Convertir entre ellos no es 1:1: el **gamut RGB es más amplio** y los colores saturados se recortan en CMYK. Aquí tienes la **conversión completa**, **perfiles de color**, **por qué falla** y **cómo preparar archivos para imprenta** sin sorpresas.

## Los cuatro espacios de color

| Espacio | Tipo | Canales | Uso | Gamut |
|---------|------|---------|-----|-------|
| **HEX** | RGB codificado | #RRGGBB | Web, CSS, diseño digital | = sRGB |
| **RGB** | Aditivo (luz) | R, G, B (0–255) | Pantallas, web, foto digital | sRGB / Display P3 / Adobe RGB |
| **HSL** | Cilíndrico (perceptual) | H (0–360°), S (0–100 %), L (0–100 %) | Diseño UI, paletas, CSS moderno | = RGB subyacente |
| **CMYK** | Sustractivo (tinta) | C, M, Y, K (0–100 %) | **Imprenta offset, digital, packaging** | **Más estrecho** |

> **HEX = RGB en hexadecimal**. `#FF8800` = `rgb(255, 136, 0)` = `hsl(32, 100%, 50%)`.  
> **CMYK ≠ inverso de RGB** matemáticamente simple: depende de **perfil de color** (papel, tinta, prensa).

## Conversiones exactas (pantalla ↔ pantalla)

### HEX ↔ RGB
```js
// HEX → RGB
const r = parseInt(hex.slice(1, 3), 16);
const g = parseInt(hex.slice(3, 5), 16);
const b = parseInt(hex.slice(5, 7), 16);

// RGB → HEX
const hex = '#' + [r, g, b].map(v => v.toString(16).padStart(2, '0')).join('');
```

### RGB ↔ HSL
```js
// RGB (0–1) → HSL
const max = Math.max(r, g, b), min = Math.min(r, g, b);
const l = (max + min) / 2;
let h, s;
if (max === min) { h = s = 0; }
else {
  const d = max - min;
  s = l > 0.5 ? d / (2 - max - min) : d / (max + min);
  switch (max) {
    case r: h = (g - b) / d + (g < b ? 6 : 0); break;
    case g: h = (b - r) / d + 2; break;
    case b: h = (r - g) / d + 4; break;
  }
  h *= 60;
}
```

> Nuestro [conversor de colores](/herramienta/color/) hace **HEX ↔ RGB ↔ HSL** al instante y exacto.

## RGB → CMYK: la conversión real (requiere perfil)

**No hay fórmula única**. La conversión usa **perfiles ICC** que modelan:
- **Tintas** (C, M, Y, K específicas de la imprenta)
- **Papel** (brillo, porosidad, blancura óptica)
- **Condiciones de impresión** (ganancia de punto, trapping, secuencia)

### Perfiles CMYK estándar (ISO 12647-2)

| Perfil | Región / Uso | Ganancia de punto (TVI) | Blancura papel |
|--------|--------------|-------------------------|----------------|
| **FOGRA39** (ISO Coated v2 300 %) | Europa, papel estucado brillante | 16 % (40 % tono) | D50, brillante |
| **ISO Coated v2 300 % (ECI)** | Europa, estándar actual | 16 % | D50 |
| **US Web Coated SWOP v2** | EE. UU., revista/web offset | 20 % | D50 |
| **Japan Color 2001 Coated** | Japón, estucado | 17 % | D50 |
| **FOGRA47** (ISO Uncoated) | Europa, papel offset sin estucar | 22 % | D50 |

> **Pregunta a tu imprenta**: "¿Qué perfil CMYK usan? (FOGRA39, SWOP, etc.)". Usa **ese** perfil al convertir.

## Por qué los colores "se apagan" en CMYK

**Gamut RGB (sRGB) vs Gamut CMYK (FOGRA39)**

```
       RGB gamut (luz)
      ╱╲
     ╱  ╲  ← Colores fuera de CMYK:
    ╱    ╲     • Azules eléctricos (#0000FF, #00FFFF)
   ╱ CMYK  ╲    • Verdes neón (#00FF00, #39FF14)
  ╱  gamut  ╲   • Naranjas/rojos muy saturados (#FF3300)
 ╱__________╲
```

**Colores problemáticos típicos:**
| RGB / HEX | En CMYK (FOGRA39) | Solución |
|-----------|-------------------|----------|
| `#0000FF` (azul puro) | Azul apagado, violáceo | Usa Pantone 286 C / 2935 C |
| `#00FF00` (verde lima) | Verde oliva sucio | Pantone 375 C / 802 C |
| `#FF00FF` (magenta RGB) | Magenta CMYK OK (es proceso) | — |
| `#FFFF00` (amarillo RGB) | Amarillo CMYK OK | — |
| `#FF8800` (naranja vivo) | Naranja marrón | Pantone 165 C / 1585 C |

## Flujo de trabajo profesional (pantalla → imprenta)

1. **Diseña en RGB** (sRGB para web, Display P3 / Adobe RGB para foto/print).
2. **Maqueta final** → convierte a CMYK **asignando el perfil de la imprenta** (no "convertir a CMYK genérico").
3. **Activa "Advertencia de gamut"** (Photoshop: Ver → Advertencia de gamut / Shift+Ctrl+Y). Los grises = fuera de gama.
4. **Ajusta manualmente** los colores fuera de gama: reduce saturación, busca alternativa en gamut, o usa **Pantone spot** para ese color crítico.
5. **Entrega PDF/X-1a** (CMYK + perfil incrustado, sin transparencias, fuentes embebidas) o **PDF/X-4** (transparencias vivas, perfil incrustado).
6. **Solicita prueba de color (hard proof)** certificada ISO 12647-7 antes de tirada.

## Tabla rápida: equivalencias aproximadas (sRGB → FOGRA39 CMYK)

| HEX / RGB | HSL | CMYK approx (FOGRA39) | Nota |
|-----------|-----|------------------------|------|
| `#FF0000` / rgb(255,0,0) | hsl(0,100%,50%) | 0, 100, 100, 0 | Rojo proceso OK |
| `#00FF00` / rgb(0,255,0) | hsl(120,100%,50%) | 100, 0, 100, 0 | Verde → fuera de gama |
| `#0000FF` / rgb(0,0,255) | hsl(240,100%,50%) | 100, 70, 0, 0 | Azul → fuera de gama |
| `#FFFF00` / rgb(255,255,0) | hsl(60,100%,50%) | 0, 0, 100, 0 | Amarillo proceso OK |
| `#FF00FF` / rgb(255,0,255) | hsl(300,100%,50%) | 0, 100, 0, 0 | Magenta proceso OK |
| `#00FFFF` / rgb(0,255,255) | hsl(180,100%,50%) | 100, 0, 0, 0 | Cian proceso OK |
| `#FFFFFF` | hsl(0,0%,100%) | 0, 0, 0, 0 | Blanco = papel |
| `#000000` | hsl(0,0%,0%) | 0, 0, 0, 100 | Negro rico: 40, 30, 30, 100 |

> **Negro rico (rich black)**: C 40 % M 30 % Y 30 % K 100 % → negro profundo sin exceso tinta.  
> **Negro solo K**: K 100 % → puede verse grisáceo en zonas grandes.

## Conclusión

- **Pantalla**: HEX = RGB = HSL (conversión exacta). Usa [conversor de colores](/herramienta/color/).
- **Impresión**: RGB → CMYK **requiere perfil ICC** (pregunta a tu imprenta).
- **Gamut**: RGB > CMYK. Colores neón/eléctricos se recortan → usa **Pantone spot** o ajusta.
- **Entrega**: PDF/X-1a o PDF/X-4 con perfil incrustado + prueba de color.

[Convierte HEX ↔ RGB ↔ HSL para pantalla](/herramienta/color/) y prepara tus CMYK con el perfil correcto.